# Use the official PHP image with Apache
FROM php:7.4-apache

# Copy the index.php file into the container's working directory
COPY . /var/www/html/

# Expose port 80 (Apache will now listen on this port)
EXPOSE 80
